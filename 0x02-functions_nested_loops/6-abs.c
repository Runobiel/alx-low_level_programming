#include "main.h"

/**
 * _abs - Entry point
 * @i: Inputed integer.
 * Description: computes the absolute value.
 * Return: absolute value
 */

int _abs(int i)
{
if (i > 0)
{
return (i);
}
else
{
return (i * -1);
}
}
