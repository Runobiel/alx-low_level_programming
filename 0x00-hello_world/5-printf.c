#include <stdio.h>
/**
 * main- entry point for the program
 *
 * Return: returns 0 if no error, else return non-zero value
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
